<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 麦克风连麦 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- 后台保活：前台服务（息屏/切后台保持语音） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- 屏幕共享：Android 14(API 34) 起 MediaProjection 必须运行在 foregroundServiceType
         含 mediaProjection 的前台服务中，且需声明本权限，否则 getMediaProjection 直接抛
         SecurityException 崩溃。flutter_webrtc 只负责弹系统授权框、不起前台服务，须由 App 补齐。 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="Oopz Live"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- 语音连麦后台保活前台服务。
             注意：manifest 声明了哪些类型，不代表启动时都会带上 —— 由 CallForegroundService
             按运行时状态动态选择位掩码：进语音只用 microphone|mediaPlayback，用户授权屏幕
             采集后才补上 mediaProjection。这是 Android 14+ 上「只进语音挂后台就断线」的修复点
             （带 mediaProjection 启动 FGS 需要屏幕采集授权，否则 startForeground 抛
             SecurityException，前台服务根本起不来）。 -->
        <service
            android:name=".CallForegroundService"
            android:foregroundServiceType="microphone|mediaPlayback|mediaProjection"
            android:stopWithTask="false"
            android:exported="false" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
